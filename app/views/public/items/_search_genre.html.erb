<table class="table">
<tr>
  <th class= "table-primary">ジャンル検索</th>
</tr>
  <% Genre.all.each do |genre| %>
    <tr>
      <td class ="table-light"><%= link_to genre.name, "/public/genres/#{genre.id}/search/" %></td>
    </tr>
  <% end %>
</table>