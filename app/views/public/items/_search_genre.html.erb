<table class="table">
<tr>
  <th>ジャンル検索</th>
</tr>
  <% Genre.all.each do |genre| %>
    <tr>
      <td><%= link_to genre.name, "/public/genres/#{genre.id}/search/" %></td>
    </tr>
  <% end %>
</table>