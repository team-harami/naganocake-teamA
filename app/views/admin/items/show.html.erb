    <h4>商品詳細</h4>
    
    <p><%= image_tag @item.item_image %></p>
    <p>商品名：<%= @item.name %></p>
    <p>商品説明：<%= @item.introduction %></p>
    <p>ジャンル：<%= @item.genre.name %>　</p>
    <p>税込価格（税抜き価格）：
        <!--税込計算-->
        <% tax_price=@item.price*1.1 %> 
        <%= tax_price.floor.to_s(:delimited)  %>（<%= @item.price %>）円</p>
    <p>
        ステータス：
        <% if @item.is_active %>
            販売中
        <% else %>
           販売停止中
        <% end %>
    </p>
    
    <%= link_to "編集する", edit_admin_item_path(@item.id) %>