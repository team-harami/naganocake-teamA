<% @items.each do |item| %>
    <p>商品id：<%= item.id %></p>
    <p>商品名：<%= item.name %></p>
    <p>税抜き価格：<%= item.price %></p>
    <p>ジャンル：<%= item.genre_id %></p>
    <p>ステータス：<%= item.genre_id %></p>
  </div>
<% end %>